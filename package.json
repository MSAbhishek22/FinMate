{
  "name": "finmate-fullstack",
  "version": "1.0.0",
  "description": "FinMate - Personal Finance Tracker",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m gunicorn --bind 0.0.0.0:$PORT app:app",
    "install-deps": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "engines": {
    "node": ">=18.0.0",
    "python": ">=3.9.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MSAbhishek22/FinMate"
  },
  "author": "MSAbhishek22",
  "license": "MIT"
}
